stencil laplacian(float grid U(0 .. x_max, 0 .. y_max))
{
	domainsize = (1 .. x_max-1, 1 .. y_max-1);
	
	
	initial
	{
	
		U[x ,y; 0] = 1;
		
	}
	
	operation 
	{
		U[x, y; t+1] =
			1 * U[x, y; t] +
			1 * (
				U[x+1, y; t] + U[x-1, y; t] +
				U[x, y+1; t] + U[x, y-1; t] 
				
			);
	}
}
