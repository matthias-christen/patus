stencil wave (
	float grid U(0 .. x_max-1, 0 .. y_max-1))
{
	iterate while t < 5;
	domainsize = (2 .. x_max-3,2 .. y_max-3);
  
	initial
	{
		
		U[x, y; 0] = 1;
		U[x, y; 1] = 0;
	}

	operation
	{
		U[x ,y; t+1] = U[x-1, y-1; t] + U[x, y; t] + U[x+1, y+1; t];	            
	}
}
